#!/bin/bash -xe

env

docker build $@ \
    --build-arg "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
	--build-arg "BUILD_VERSION=$(git describe --tags --long --dirty --always)" \
    --build-arg "VCS_REF=$(git rev-parse --short HEAD)" \
	--file "${DOCKERFILE_PATH}" \
	--tag "${IMAGE_NAME}" \
	.