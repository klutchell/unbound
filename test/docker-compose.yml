version: '3'
services:
    unbound:
        image: klutchell/unbound
    drill:
        image: alpine
        command: sh -c 'apk add -q --no-cache drill && drill -p 5053 nlnetlabs.nl @unbound'
        links:
            - unbound